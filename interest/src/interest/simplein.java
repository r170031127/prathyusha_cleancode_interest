package interest;

public class simplein {
 public int simple(int principle_amount,int time,int rate_of_interest)
 {
	 int result = (principle_amount*time*rate_of_interest)/100;
	 return result;
 }
}
